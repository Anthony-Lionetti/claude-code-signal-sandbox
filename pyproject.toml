[project]
name = "interview-playground"
version = "0.1.0"
description = "CodeSignal ICA Practice Problems"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
